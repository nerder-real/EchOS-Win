# EchOS-Win v1.0.9

本次共八项修复，集中在四块：**分流模式**相关的交互与断网、启动与系统代理的
时机、**桌面图标**显示异常，以及**安装包**自身的图标替换与缓存处理。

| # | 问题 | 影响 |
| --- | --- | --- |
| 一 | 切换分流模式后药丸高亮不跟随 | 看起来像"点了没反应" |
| 二 | 重启代理期间拆掉系统代理 | 程序短暂断网 |
| 三 | 启动要等自检通过才接管代理 | 启动后十几秒上不了网 |
| 四 | 快捷方式未显式指定图标 | 依赖 exe 抠图，易出残影 |
| 五 | 图标黑边、杂色与小尺寸发虚 | 桌面图标"脏" |
| 六 | 还原系统代理到 10808 | 退出后代理指向失效端口 |
| 七 | 覆盖安装后图标缓存不刷新 | 重装了还是老样子 |
| 八 | 便携版图标一直是 7-Zip 的 | 免安装版显示错误的图标 |

## 修复一：切换分流模式后药丸不跟随高亮

现象：代理运行中切换「绕过中国大陆 / 黑名单 / 全局」，日志显示切换成功、内核也
按新模式重启了，但界面上的高亮药丸停在原来的位置，看起来像"点了没反应"。

原因在 Flutter 的重建机制上。主页上的模式切换控件是以 `const` 实例挂上去的，
父级重建时 Flutter 发现 widget 实例相同（`identical`）就会跳过整棵子树 ——
控件根本没机会读到新的 `routeMode`。

改法是让控件自己订阅状态：用 `AnimatedBuilder(animation: app, ...)` 包一层，
模式变化由它自己响应，不再依赖父级传下来的重建。主页上另外六个控件经排查在
使用处都没有 `const` 前缀，不受影响，只改这一处。

顺带把切换日志写清楚：会记录"从哪个模式 → 哪个模式"、代理未运行时说明下次启动
生效、重启结束后说明结果（以前失败时日志停在"正在重启…"，看不出到底成没成）。

## 修复二：重启代理期间不再拆掉系统代理

这是"用着 EchOS，某些程序（比如 WorkBuddy）突然断网"的根因。

### 问题

切换分流模式、切换服务器、应用自定义规则、保存参数后重启 —— 这些操作都要重启
内核。每次重启的几秒钟里，走系统代理的程序会连不上网。

### 根因

原来的流程是：

```
切模式 → 停内核 → 还原系统代理 → 启动新内核 → 等端口就绪 → 自检 → 重新接管系统代理
                  ↑ 这里就断了            ↑ 到这里才恢复
```

本地端口（30000 / 30001）是固定的，新内核起来后原来的系统代理设置本来依然有效，
"还原再接管"这中间的一步其实是多余的。而这一步的代价很实在：中间那 **2~6 秒**
系统代理是**关闭**状态，此时程序直连境外域名必然超时，表现就是"什么都没干，
网络突然断了"。

（补充：还原时写回的是接管前的系统代理设置。如果那份备份里的代理程序已经不在
了，这段时间的断网会更彻底。）

### 现在的做法

重启内核时**保留系统代理不动**：

- 内核重启本身仍有 1~6 秒空档，但程序拿到的是 `connection refused`（快速失败，
  多数程序会自己重试），远好过几十秒的直连超时。
- `start()` 有多条提前返回的路径（没选服务器 / 配置不完整或未保存 / 内核起不来 /
  端口被占用 / 端口一直未就绪）。任何一条失败都会留下"系统代理指向已经死了的本地
  端口"，比直接断网更糟，所以加了兜底：重启后若代理没跑起来，就还原系统代理。
- 同时给内核进程加了「代号」。`stop → start` 时旧内核的退出回调往往晚到，会把刚
  起来的新内核状态打回去。现在旧内核的迟到退出会被忽略。

一并修掉的四处场景（它们的性质完全相同：都是重启内核、端口不变）：

| 操作 | 说明 |
| --- | --- |
| 切换分流模式 | 绕过中国大陆 / 黑名单 / 全局 |
| 切换服务器 | 换一个节点 |
| 应用自定义规则 | 保存规则后重启 |
| 保存参数后重启 | 「保存」按钮触发的重启 |

**用户主动关闭代理、退出程序、自检失败自动关闭**等路径不受影响，仍然照旧还原
系统代理，不会在退出后留下失效的代理设置。

## 修复三：启动与自检解耦，代理启动后不再长时间上不了网

「启动了代理，但要等好一阵网页才打得开」——原因是启动流程把两件事缠在了一起：

```
旧：内核起来 → 等端口就绪 → 跑自检(隧道探针) → 通过后才接管系统代理
                                  ↑ 慢节点能拖十几秒
新：内核起来 → 等端口就绪 → 立刻接管系统代理
                        → 自检照跑，只负责判断隧道通不通，不通就回滚
```

实测一次启动里，从端口就绪到真正接管隔了 **22 秒**，这段时间系统代理是关的，
浏览器直连，境外站点全挂。现在这段窗口压到了端口就绪的耗时。

自检并没有被削弱，只是不再当"准入门槛"：

- 隧道通 → 一切照旧
- 隧道不通 → 自检失败走 `stop()`，会自动还原系统代理，不会留下指向死端口的
  代理设置。因为提前接管了，失败时走的是"已接管 → 关闭并还原"这条更干脆的分支

## 修复四：桌面快捷方式图标显式指定

桌面和开始菜单的快捷方式原先都没写 `IconFilename`，靠 Windows 从 `echos.exe`
里抠图标、再进图标缓存。抠图或缓存任一环节出问题（重装时 exe 正在被替换最容易
中招），桌面就显示成残影、杂色，或者把低分辨率小图拉伸成大图标。

现在改为随包安装一份 `logo.ico` 到安装目录，快捷方式显式指向它，与 exe 解耦。

## 修复五：图标黑边、杂色与小尺寸发虚

这是上一版「重编码为 BMP 条目」时引入的**新问题**，也是"桌面图标脏"的直接成因。

### 现象

图标边缘（尤其圆角处）出现黑色杂边、残影，看起来像没擦干净的旧图。

### 根因

ICO 里每个 BMP 条目除了 32bpp 的 BGRA 数据（XOR），还带一张 1bpp 的 **AND
掩码**：某位为 1 表示"这个像素透明，别画"。

Windows 有两条渲染路径：现代路径按 alpha 通道做混合，老式 GDI 路径**只看 AND
掩码**、完全不理会 alpha。上一版重编码时掩码被写成全 0 —— 等于声明"一个透明像素
都没有"。于是老式路径把四个圆角的透明区当成实色画了出来，而那些像素的 RGB 是 0，
画出来就是**黑角**。

逐项核对的结果（掩码置位数应等于透明像素数）：

| 尺寸 | 掩码置位数 | 实际透明像素 | 结论 |
| --- | --- | --- | --- |
| 16×16 | 0 | 4 | 掩码失效 |
| 48×48 | 0 | 48 | 掩码失效 |
| 256×256 | 0 | 1846 | 掩码失效 |

### 修复

按 `alpha == 0 → 掩码位置 1` 重新生成各尺寸档位的掩码，并与 alpha 通道逐像素核对：

| 尺寸 | 掩码置位数 | 透明像素 | 不符 |
| --- | --- | --- | --- |
| 16 / 24 / 32 / 48 / 64 / 128 / 256 | 4 / 9 / 20 / 48 / 108 / 478 / 1846 | 同左 | **0** |

同时确认了另外两件事，排除其它嫌疑：

- 重编码是**逐像素无损**的：BMP 版与原 PNG 版的半透明像素数逐档完全一致
  （1777 / 541 / 244 / 188 / 116 / 87 / 56），抗锯齿边缘没有丢失。
- 透明像素的 RGB 全是 0，不存在"透明区残留旧颜色"这类问题。

### 另外三个成因（一并修掉）

掩码解释了"黑角"，但"脏"不止这一个来源。逐项排查后又修掉三处：

**1. 尺寸档位不全。** 原来只有 7 档（16 / 24 / 32 / 48 / 64 / 128 / 256），而
Windows 实际会按 20、28、40、60、72、80、96 这些中间档位取图。缺档时 Explorer
只能拿最近的档位实时缩放，边缘必然发虚。现在补齐到 **14 档**：
16 / 20 / 24 / 28 / 32 / 40 / 48 / 60 / 64 / 72 / 80 / 96 / 128 / 256。

**2. 缩放算法的振铃效应。** 缩小用 LANCZOS 会在急剧边缘产生过冲/下冲，大图上看
是锐利，缩到 16×16 就变成**散落的暗斑点** —— 这是"小图标脏"的主因。现在缩小一
律改用**面积平均**（纯低通，不过冲），只有放大到 256 仍用 LANCZOS；重采样前先做
alpha 预乘，否则透明像素的 RGB(0) 会混进边缘形成暗边。

**3. 小尺寸"简化"已尝试并回退。** 一度对 ≤32px 档位按饱和度/亮度去掉"地面"与
投影，但云底部的浅灰阴影正好落在「低饱和 + 亮度 < 230」这个判定区间，会被一并
刷成橙色，结果 16~32px 上**云缺掉一半**（24×24 实测有一整行云被吞掉）。
阴影本就是 logo 设计的一部分，所以最终**全部档位都保留完整设计**，不再做简化。

## 修复六：不再把系统代理还原到 10808

退出 EchOS 后，系统代理被还原成 `127.0.0.1:10808`。这个地址是 v2rayN 退出时留在
注册表里的僵尸值：代理开关本来是关的，所以它平时不生效，但 EchOS 会把"接管前读
到的 ProxyServer"原样写回去，看上去就像还原到了一个陌生地址。

现在的规则是：**只有接管前系统代理确实处于启用状态，才写回地址**；否则直接删掉
`ProxyServer` 这个僵尸值。

这样有两个好处：日志不再出现莫名其妙的地址；以后手动打开系统代理开关时，也不会
意外连到一个早就失效的端口。

还原日志也跟着改清楚了，会明确区分是「沿用接管前的配置」还是「关闭」。

## 修复七：覆盖安装后自动刷新图标缓存

这是「重装了还是老样子」的由来。

### 现象

覆盖安装了新版本，桌面快捷方式还是旧图标（或者还是脏的）；手动删掉快捷方式再装
也一样。

### 根因

Explorer 是**按文件路径**缓存图标的。覆盖安装时 `logo.ico` 的内容换了、路径没换，
Explorer 常常直接沿用缓存里的旧位图 —— 文件是新的，画出来的还是旧的。

### 做法

安装完成（快捷方式已建好或更新完）后调用 `ie4uinit.exe -ClearIconCache`，把缓存
条目标记为失效，Explorer 下次绘制时自动重新提取图标。

**刻意没有重启资源管理器。** `taskkill /f explorer` 再拉起很容易让任务栏、托盘
通知区恢复不全（表现为任务栏卡死或整个不显示），代价远大于收益；清缓存的效果
等价，但温和得多。

## 修复八：便携版显示的其实是 7-Zip 图标

免安装的便携版一直显示 7-Zip 的图标，不是 EchOS 的。

### 根因

便携版是「SFX 模块 + 配置 + 归档」拼出来的单文件，图标靠 ResourceHacker 换。
7zSD 系列 SFX 自带的图标组 id 就是 **101**，而脚本里用的 mask 是
`ICONGROUP,MAINICON` —— 它**不覆盖** 101，而是另建一个组。Windows 取图标时按
资源 id 升序取第一个组，于是拿到的永远是 101 那个 7-Zip 的，新注入的图标组形同
虚设。

实测旧包里有两个图标组：

| 组 id | 档位 | 指向 |
| --- | --- | --- |
| 101 | 4 档（16 / 24 / 32 / 48） | 7-Zip 自带图标 |
| 2147484816 | 14 档 | EchOS 图标（未被采用） |

### 修复

mask 改为 `ICONGROUP,101`，直接覆盖自带组。修复后只剩一个组、14 档全部指向
EchOS 图标。CI 与本地构建脚本同处此问题，一并修正；换用其它 SFX 模块时需要重新
确认这个组 id。

## 验证

- `flutter analyze`：零问题
- 本地完整构建：内核、Flutter、Inno Setup、便携版四阶段全通
- 产物确认：便携版内 `app.so` 与构建产物逐字节一致（MD5 比对），版本号 `1.0.9`
  与 `ECHOS_REPO` 注入正确
- 内核 MD5 与源码编译结果一致（`9ad8fbea…`）
- 安装版重新编译通过（Inno Setup 17 秒，`RefreshIconCache` 编译无误）
- `echos.exe` 内嵌图标与 `app_icon.ico` 逐资源比对 **14/14 一致**（14 档全部命中，
  不是抽样）
- 便携版重建后仅剩 1 个图标组，14 档全部指向 EchOS 图标；从中抽出的 `data/app.so`
  与 Release 的 MD5 一致（`64659fe9…`），确认拼接无损

## 下载哪个

| 文件 | 说明 |
| --- | --- |
| `EchOS-Win-v1.0.9-x64-Setup.exe` | 安装版。写入「应用和功能」，推荐日常使用 |
| `EchOS-Win-v1.0.9-x64-Portable.exe` | 便携版。单文件自解压，免安装，适合 U 盘携带 |

## 升级说明

- 覆盖安装即可，配置存放在 `%APPDATA%\EchOS`，不受影响。
- 分流模式的选择会持久化：新装默认是「绕过中国大陆」，你改过之后下次启动沿用
  你上次的选择。
- 自报版本 1.0.8 及以下的客户端均可收到本次更新。

---

完整提交历史：https://github.com/nerder-real/EchOS-Win/commits/v1.0.9
